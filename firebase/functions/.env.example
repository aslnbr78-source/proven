# Copy to .env in this folder (firebase/functions/.env)
# Get a key from https://aistudio.google.com/apikey — NOT from Firebase Console.
#
# New keys start with AQ.  Legacy keys start with AIza.
# Do not use VITE_FIREBASE_API_KEY from the project root .env.

GOOGLE_AI_API_KEY=AQ.your_key_here
