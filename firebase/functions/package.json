{
  "name": "provenmath-functions",
  "description": "ProvenMath LMS Cloud Functions",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^10.3.0",
    "firebase-functions": "^4.9.0"
  }
}
